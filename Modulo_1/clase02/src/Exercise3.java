public class Exercise3 {
    public static void main(String[] args) {
        // Definimos la cantidad de peces rojos y azules
        int redFish = 284;
        int blueFish = 63;  

        // Calculamos el total de peces en el acuario
        int totalFish = redFish + blueFish;

        // Mostramos el total de peces en el acuario
        System.err.printf("There are: %d fish in the aquarium.", totalFish);
    }
}
